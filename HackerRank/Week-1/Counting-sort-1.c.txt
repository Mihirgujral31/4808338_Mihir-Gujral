#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);
    int freq[100]={0};  
    for (int i=0; i<n; i++) {
        int val;
        scanf("%d", &val);
        freq[val]++;  
    }
    for (int i=0; i<100; i++) {
        printf("%d ", freq[i]);
    }
    return 0;
}
